<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug builds may use adb reverse to reach the host Gateway on emulator localhost. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
