'use strict';
const $=id=>document.getElementById(id);let timer=null,busy=false;
function text(value){return value===null||value===undefined||value===''?'—':String(value)}
function setCard(id,state,detail,good){$(id).textContent=text(state);$(id+'Detail').textContent=text(detail);$(id).style.color=good?'#70e0b4':good===false?'#ff9da6':''}
function render(data){const p=data.provider||{},ph=data.provider_health||{},g=data.gateway||{},l=data.legacy||{},t=data.talk||{};const pgood=p.ActiveState==='active'&&ph.status==='ok';const ggood=g.ok===true&&g.nemotron_loaded===true;const legacyOff=l.ActiveState!=='active';setCard('providerState',ph.status||p.ActiveState,p.ActiveState+' · PID '+text(p.MainPID),pgood);setCard('gatewayState',g.ok?'Healthy':'Unavailable',g.nemotron_loaded?'Nemotron plugin loaded':(g.plugin_errors||[]).join(', '),ggood);setCard('talkState',t.provider||'—',`${text(t.mode)} · ${text(t.transport)} · ack ${JSON.stringify(t.ackMessages||[])}`,t.provider==='nemotron-realtime-voice');setCard('legacyState',legacyOff?'Stopped':'Running',text(l.ActiveState)+' · '+text(l.SubState),legacyOff);const overall=pgood&&ggood&&legacyOff;$('overall').textContent=overall?'Healthy':'Needs attention';$('overall').className='pill '+(overall?'ok':'bad');const fields=[['provider',t.provider],['mode',t.mode],['brain',t.brain],['transport',t.transport],['consult routing',t.consultRouting],['server URL',t.serverUrl],['ack messages',JSON.stringify(t.ackMessages||[])],['emit user transcript',t.emitUserTranscript],['emit assistant transcript',t.emitAssistantTranscript],['plugin revision',data.plugin_revision]];$('routing').replaceChildren(...fields.map(([k,v])=>{const d=document.createElement('div'),dt=document.createElement('dt'),dd=document.createElement('dd');dt.textContent=k;dd.textContent=text(v);d.append(dt,dd);return d}));if(data.job?.state==='running'){$('operation').textContent=`${data.job.kind}: ${data.job.name} running…`;schedule(1000)}else if(data.job?.state&&data.job.state!=='idle'){$('operation').textContent=`${data.job.kind}: ${data.job.name} ${data.job.state} (exit ${text(data.job.exit_code)})`;if(data.job.output){$('testOutput').hidden=false;$('testOutput').textContent=data.job.output}}else $('operation').textContent='No operation running.'}
async function load(){try{const r=await fetch('/api/nemotron/status',{cache:'no-store'}),d=await r.json();if(!r.ok)throw Error(d.error||'Status unavailable');render(d)}catch(e){$('overall').textContent='Unavailable';$('overall').className='pill bad';$('operation').textContent=e.message;$('operation').className='notice error'}finally{schedule(3000)}}function schedule(ms){clearTimeout(timer);timer=setTimeout(load,ms)}async function post(kind,name){if(busy)return;busy=true;$('operation').textContent=`${kind}: ${name} starting…`;try{const r=await fetch('/api/nemotron/operations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({kind,name})}),d=await r.json();if(!r.ok)throw Error(d.error||'Operation failed');$('operation').className='notice ok';await load()}catch(e){$('operation').textContent=e.message;$('operation').className='notice error'}finally{busy=false}}
document.querySelectorAll('[data-action]').forEach(b=>b.onclick=()=>post('action',b.dataset.action));document.querySelectorAll('[data-test]').forEach(b=>b.onclick=()=>post('test',b.dataset.test));$('refresh').onclick=load;$('loadLogs').onclick=async()=>{const r=await fetch('/api/nemotron/logs'),d=await r.json();$('logs').hidden=false;$('logs').textContent=d.text||d.error||'No logs.'};load();
